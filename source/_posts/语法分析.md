---
title: 语法分析
date: 2022-9-26 13:17:20
updated:
tags: compiler
categories:
keywords: compiler
description:
top_img: https://raw.githubusercontent.com/hjc-owo/hjc-owo.github.io/img/202209261318355.jpg
comments:
cover: https://raw.githubusercontent.com/hjc-owo/hjc-owo.github.io/img/202209261318355.jpg
toc: true
toc_number:
toc_style_simple:
copyright:
copyright_author: 花开一季 叶落一地
copyright_author_href: https://github.com/hjc-owo
copyright_url:
copyright_info:
mathjax:
katex:
aplayer:
highlight_shrink:
aside:
---

# 语法分析设计

## 语法分析综述

在前面的词法分析的基础上，考虑到实现后续功能的便捷性，我们在此处建立了一棵语法树。

当然，在使用递归下降的方法进行语法分析的要求是文法当中不存在左递归，同时尽可能的避免回溯。下面对这些情况进行考虑。

### 左递归

考虑到所提供的部分文法当中存在左递归，例如文法`AddExp -> MulExp | AddExp ('+' | '−') MulExp`中，可采用转换为扩展的 BNF 范式形式的方法予以解决，我们可以将其改写成`AddExp -> MulExp { ('+' | '−') MulExp }`，即可避免左递归的问题。

但是由于我们改写了文法，相应的语法树也会有所改变。

![截屏2022-09-26 11.30.59](https://raw.githubusercontent.com/hjc-owo/hjc-owo.github.io/img/202209261131986.png)

比如上面这种情况，语法树的层数发生了改变，导致语法分析阶段输出结果的时候会少输出一些 \<AddExp>，可以通过调整输出 \<AddExp> 的位置来解决。

```java
public void print() {
    for (int i = 0; i < mulExpNodes.size(); i++) {
        mulExpNodes.get(i).print();
        IOUtils.write(Parser.nodeType.get(NodeType.AddExp));
        if (i < operations.size()) {
            IOUtils.write(operations.get(i).toString());
        }
    }
}
```

也就是说，把输出 \<AddExp> 的句子放在每个`mulExpNode.print()`的后面即可。

### 回溯问题

回溯问题的存在是因为对于某个非终结符号的规则其右部有多个选择，且其first集相交，从而导致分析到此语法成分时，不能仅根据当前词法成分判断接下来的语法成分是什么。 

例如`VarDef -> Ident { '[' ConstExp ']' } | Ident { '[' ConstExp ']' } '=' InitVal`就出现了回溯问题，我们想要避免这个回溯问题也很简单，只需要改写文法使之变成`VarDef -> Ident { '[' ConstExp ']' } [ '=' InitVal ]`即可解决回溯问题。

类似的还有 Stmt 文法。但是考虑到 Stmt 文法比较复杂，我们在后面拿出专门的一节再来说这个问题。

## 语法分析文件结构

```
.
├── Compiler.java
├── frontend
│   ├── Lexer.java
│   └── Parser.java
├── node
│   ├── AddExpNode.java
│   ├── BTypeNode.java
│   ├── BlockItemNode.java
│   ├── BlockNode.java
│   ├── CompUnitNode.java
│   ├── CondNode.java
│   ├── ConstDeclNode.java
│   ├── ConstDefNode.java
│   ├── ConstExpNode.java
│   ├── ConstInitValNode.java
│   ├── DeclNode.java
│   ├── EqExpNode.java
│   ├── ExpNode.java
│   ├── FuncDefNode.java
│   ├── FuncFParamNode.java
│   ├── FuncFParamsNode.java
│   ├── FuncRParamsNode.java
│   ├── FuncTypeNode.java
│   ├── InitValNode.java
│   ├── LAndExpNode.java
│   ├── LOrExpNode.java
│   ├── LValNode.java
│   ├── MainFuncDefNode.java
│   ├── MulExpNode.java
│   ├── NodeType.java
│   ├── NumberNode.java
│   ├── PrimaryExpNode.java
│   ├── RelExpNode.java
│   ├── StmtNode.java
│   ├── UnaryExpNode.java
│   ├── UnaryOpNode.java
│   ├── VarDeclNode.java
│   └── VarDefNode.java
├── token
│   ├── Token.java
│   └── TokenType.java
└── utils
    └── IOUtils.java
```

## 每个部分作用

### node 文件夹

在 node 文件夹内是所有的非终结符号作为的树结点。

以文法`CompUnit -> {Decl} {FuncDef} MainFuncDef`为例。

```java
public class CompUnitNode {
    // CompUnit -> {Decl} {FuncDef} MainFuncDef

    private List<DeclNode> declNodes;
    private List<FuncDefNode> funcDefNodes;
    private MainFuncDefNode mainFuncDefNode;

    public CompUnitNode(List<DeclNode> declNodes, List<FuncDefNode> funcDefNodes, MainFuncDefNode mainFuncDefNode) {
        this.declNodes = declNodes;
        this.funcDefNodes = funcDefNodes;
        this.mainFuncDefNode = mainFuncDefNode;
    }

    public void print() {
        for (DeclNode declNode : declNodes) {
            declNode.print();
        }
        for (FuncDefNode funcDefNode : funcDefNodes) {
            funcDefNode.print();
        }
        mainFuncDefNode.print();
        IOUtils.write(Parser.nodeType.get(NodeType.CompUnit));
    }

}
```

`{Decl}`表示`Decl`有可能有零个到多个，我们选择使用`List<DeclNode>`来存储所有的 Decl 的树结点。类似的使用`List<FuncDefNode>`来存储所有的 FuncDefNode 的树结点。

## `Parser`

文件结构如下所示。

```java
public class Parser {
    private List<Token> tokens; // 词法分析的结果
    private int index = 0; // 在 token 列表的下标
    private CompUnitNode compUnitNode; // 语法树的根结点

    public Parser(List<Token> tokens) {
        this.tokens = tokens;
    }

    public void analyze() {
        this.compUnitNode = CompUnit();
    }

    public static Map<NodeType, String> nodeType = new HashMap<NodeType, String>() {{
        put(NodeType.CompUnit, "<CompUnit>\n");
        put(NodeType.Decl, "<Decl>\n");
        ...
    }};

    public enum StmtType {
        LValAssignExp, Exp, Block, If, While, Break, Continue, Return, LValAssignGetint, Printf
    }

    ... // 对每条文法进行分析
      
    private Token match(TokenType tokenType) {
        if (tokens.get(index).getType() == tokenType) {
            return tokens.get(index++);
        } else {
            throw new RuntimeException(...);
        }
    }

    public void printParseAns() {
        // 输出语法分析部分答案
        compUnitNode.print();
    }
}
```

`Map<NodeType, String> nodeType`的作用是将要输出的语法成分的名字放在一个 Map 里面，直接使用`nodeType.get()`方法可以方便的进行输出。同时如果以后需要修改输出的语法成分的名字等，也只需要修改这一个地方即可完成。

`Token match(TokenType tokenType)`是判断当前 tokens 列表当中下标为 index 的元素的 type 是否与所期望的 tokenType 相同。如果相同则表示这个语法成分是正确的，否则就给出 RuntimeException，同时输出相关错误信息。

`printParseAns()`即为输出语法分析阶段答案的方法，通过遍历语法树的方式进行答案的输出。

### Stmt 文法

```
Stmt -> LVal '=' Exp ';'
	| [Exp] ';'
	| Block
	| 'if' '(' Cond ')' Stmt [ 'else' Stmt ] 
	| 'while' '(' Cond ')' Stmt
	| 'break' ';' | 'continue' ';'
	| 'return' [Exp] ';'
	| LVal '=' 'getint' '(' ')' ';'
	| 'printf' '(' FormatString { ',' Exp } ')' ';'
```

先看 Stmt 树结点的构造。

```java
public class StmtNode {
    private Parser.StmtType type;
    private LValNode lValNode;
    private ExpNode expNode;
    private BlockNode blockNode;
    private CondNode condNode;
    private List<StmtNode> stmtNodes;
    private Token formatString;
    private List<ExpNode> expNodes;

    public StmtNode( ... ) { ... } // 构造方法

    public void print() {
        switch (type) { // 使用 type 控制输出
            case LValAssignExp:
                ...
            ...
        }
    }
}
```

其中 StmtType 是一个枚举，正如上面所见，考虑到 Stmt 文法分支太多，使用 type 来方便的控制输出。

```java
public enum StmtType {
    LValAssignExp, Exp, Block, If, While, Break, Continue, Return, LValAssignGetint, Printf
}
```

在 Parser.java 当中，对 Stmt 文法处理的流程图如下所示。

![截屏2022-09-26 13.11.22](https://raw.githubusercontent.com/hjc-owo/hjc-owo.github.io/img/202209261311440.png)

Stmt 文法处理代码结构如下所示。

```java
private StmtNode Stmt() {
    StmtType stmtType = null;
    LValNode lValNode = null;
    ExpNode expNode = null;
    BlockNode blockNode = null;
    CondNode condNode = null;
    List<StmtNode> stmtNodes = new ArrayList<>();
    Token formatString = null;
    List<ExpNode> expNodes = new ArrayList<>();
    if (tokens.get(index).getType() == TokenType.LBRACE) {
        // Block
        ...
    } else if (tokens.get(index).getType() == TokenType.PRINTFTK) {
        // 'printf' '(' FormatString { ',' Exp } ')' ';'
        ...
    } else if (tokens.get(index).getType() == TokenType.IFTK) {
        // 'if' '(' Cond ')' Stmt [ 'else' Stmt ]
        ...
    } else if (tokens.get(index).getType() == TokenType.WHILETK) {
        // 'while' '(' Cond ')' Stmt
        ...
    } else if (tokens.get(index).getType() == TokenType.BREAKTK) {
        // 'break' ';'
        ...
    } else if (tokens.get(index).getType() == TokenType.CONTINUETK) {
        // 'continue' ';'
        ...
    } else if (tokens.get(index).getType() == TokenType.RETURNTK) {
        // 'return' [Exp] ';'
        ...
    } else {
        ... // 计算 assign 和 semicn
        if (assign < semicn && assign > index) {
            ...
            if (tokens.get(index).getType() == TokenType.GETINTTK) {
                // LVal '=' 'getint' '(' ')' ';'
                ... 
            } else {
                // LVal '=' Exp ';'
                ... 
            }
            ...
        } else {
            // [Exp] ';'
            ...
        }
    }
    return new StmtNode( ... );
}
```